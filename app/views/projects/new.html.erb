<%= form_for(@project, :url => {:action=>:create}) do |f|%>
  <%= f.label :owner %>
  <%= f.text_field :owner %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.hidden_field :project_status_id, :value => 1%>

  <%= f.submit "登録する"%>
<% end %>
